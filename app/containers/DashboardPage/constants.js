import { DASHBOARD_URL } from '../../common/constants';

export const SUB_MENU_BASE = [
    { url: DASHBOARD_URL, display: 'Recent' },
];

export const PAGINATION_PREPEND = 'prepend';
export const PAGINATION_APPEND = 'append';
